// Children interface
export interface ChildrenProps { children?: React.ReactNode; }

// Margin interface
export interface MarginProps {
    all?: number;
    top?: number;
    bottom?: number;
    left?: number;
    right?: number;
}